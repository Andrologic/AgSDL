# Specification

This directory will contain the normative AgSDL specification.

The specification has not been drafted yet. Initial work belongs in `docs/` and
`proposals/` until the terminology, system model, and conformance boundaries have
been reviewed.

The specification defines portable document meaning and requirements for
independent implementations. An implementation's internal design is not
normative AgSDL semantics.
