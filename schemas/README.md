# Schemas

This directory will contain machine-readable schemas after AgSDL selects its
serializations.

Schemas validate the normative constraints expressible in the selected schema
language. They do not determine how constraints outside that language are
checked. If a schema and the specification disagree, the specification controls
and the mismatch is a defect.
