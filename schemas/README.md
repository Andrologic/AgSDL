# Schemas

This directory will contain machine-readable schemas after AgSDL selects its
serializations.

Schemas validate representations of the normative model. If a schema and the
specification disagree, the specification controls and the mismatch is a defect.
