# Local preparation for AgSDL 0.1.0

- Date: 2026-09-05.
- Status: non-normative preparation, integrated locally; not a release.
- Starting AgSDL `develop`: `7eb118cbd04a014c9f17bb1520a6cfc2f126dda9`.
- Integrated content checkpoint: `dab7a8ef875d1e31817e53de677b7eab9914bbdc`.

## Authorization and boundaries

After the 0.1.0 planning discussion, the maintainer authorized grouping and
executing only the least conceptually risky work autonomously. The subsequent
model instruction selected GPT-6 Astra with low reasoning. Production and
independent reviews used that profile. Commits and merges stay local; the
earlier authorization to publish 0.0.2 is not reused for this preparation.

The maintainer's engine directions are open implementations, including future
custom engines, and no default engine. Those directions do not select an
identifier syntax, capability contract, or runtime architecture. The exact
0.1.0 contract still requires review. All eleven proposals remain proposed.
The [repository scope](../scope.md) and [Decision 0004](../decisions/0004-approved-design-directions.md)
continue to distinguish preparation from normative adoption.

Only preparatory portions of user-plan items 2, 7, 10, 22-31 and 35 were
executed in AgSDL. No parser, schema, execution adapter or conformance suite
was implemented. No Macro file was modified.

## Lots and ownership

| Lot | Result and plan references | State at checkpoint |
| --- | --- | --- |
| A | [Eleven candidate scenarios](../research/0.1.0-acceptance-scenarios.md) and [implementation evidence prerequisites](../research/0.1.0-implementation-checklist.md), preparatory parts of 2, 7, 10 and 35. | Integrated. Candidate outcomes remain conditional on adopted rules. |
| B | [Runtime engine interface research](../research/runtime-engine-interfaces.md), preparatory parts of 22-31. | Integrated. Published interfaces and source snapshots are evidence of documentation, not tested support. |
| C | Reproduce and correct definite defects in the existing Studio codec, item 34. | Blocked before production. No native task/worktree was established. |

Two production slots were planned, with independent review capacity. No browser,
server, agent engine or heavy build was started by this AgSDL preparation.
The orchestrator owned the integration register and serialized merges. A and B
owned distinct research files; neither edited shared proposals or normative text.

| Lot | Native task on host `local` | Native worktree | Feature branch |
| --- | --- | --- | --- |
| A | `01a072a2-d36d-7c60-8263-df4507dd5e44` | `/Users/oscarlahaie/.codex/worktrees/6f32/AgSDL` | `feature/010-acceptance-preparation` |
| B | `01a072a2-e67c-7412-bcac-fdf20961cd2c` | `/Users/oscarlahaie/.codex/worktrees/e673/AgSDL` | `feature/010-runtime-interface-research` |

Both initial native checks reported their own clean detached worktree at the
starting AgSDL SHA. The orchestrator verified the initial command directories,
Git worktree registrations and base before dispatching production. The native
list omitted the new tasks; local application creation logs supplied candidate
IDs, then `read_thread` confirmed each original lot prompt, title and directory.
No manual worktree substituted for task attachment.

## Reviews and integration evidence

| Lot or review | Base | Candidate | Independent auditor and result |
| --- | --- | --- | --- |
| A | `7eb118cbd04a014c9f17bb1520a6cfc2f126dda9` | `9bdbfe1818b5116969795deb072e65dc876fcb2b` | `/root/acceptance_review`, native subagent `01a072aa-4a88-7700-b9c4-c20ba6194e82`: favorable, no findings. |
| B initial | `7eb118cbd04a014c9f17bb1520a6cfc2f126dda9` | `5adfc6e99afa13f249284913d3db921125518d85` | `/root/review_runtime`, `01a072aa-5b08-7803-b4e9-72765ab9b05f`: unfavorable, one factual defect. |
| B corrected full document | `7eb118cbd04a014c9f17bb1520a6cfc2f126dda9` | `a461fa2155b888a2876e24751dac77f8a5c38635` | `/root/review_runtime_correction`, `01a072ac-109b-7441-8572-cf46d2b61c20`: favorable. |
| B final correction | `a461fa2155b888a2876e24751dac77f8a5c38635` | `d89cafc6bba20e7defbf537d7bc1b7bba9b49f93` | `/root/review_sigterm`, `01a072af-57ac-7e01-89c4-593e507e4122`: favorable on the two-line correction. |

All reviewers used fresh contexts and worked in read-only mode. A's reviewer
checked the eleven cases, source traceability, open decisions and separation of
operations. It found no invented syntax, executed fixture or adopted contract.
It did not run tests; the production task supplied the required check results.

B's first reviewer found that permission requests were incorrectly grouped with
Codex's experimental dynamic-tool opt-in. The task corrected this statement;
a fresh reviewer assessed the complete revised document and its primary sources.
The orchestrator then found a separate omission: the cited Claude CLI source
does document SIGTERM behavior. The task corrected the cancellation cell and
source index. A third fresh reviewer confirmed the exact delta against the
official section, preserving the distinction between stopping a process and
rolling back external effects. The reviews establish documentary accuracy
within their scope, not executable interoperability.

The orchestrator read the complete A/B documents, checked the changed paths,
verified the Claude cancellation source directly, and inspected the independent
reports before integration. B added a separate file after A; the changes had
no textual conflict and their non-normative boundaries agree.

- A merged as `ff0ab5cf45047decfa63983095a39c05845dd6db`.
- B merged as `dab7a8ef875d1e31817e53de677b7eab9914bbdc`.
- `./scripts/check.sh` passed on the initial checkout, on the production
  candidates and after each merge. `git diff --check` passed. These remain
  repository maintenance checks, not language conformance evidence.
- At the content checkpoint, `develop` was clean and six commits ahead of the
  locally recorded `origin/develop`. `main` and the existing version tags were
  unchanged. No push, release tag, publication or deployment was performed.

## Studio startup blocker and remaining decisions

The Studio checkout was clean on `develop` at
`6d8c12de423a1583dbc4841cacf1a3b4d7a78f8d`. Native creation returned client ID
`client-new-thread:cb00e581-b8dc-4ad5-b4a9-5698a252565c`, then failed before a
task was established: the application attempted to create a worktree from
`main`, which does not exist in that repository. The local creation log records
exit 128 at 2026-09-05T17:44:40.086Z. No codec fix or Studio validation is claimed.

The creation tool requires an explicit requested starting state to override its
default. A question to start C from `develop` is pending. This is a task-startup
constraint, not a conceptual decision or evidence that the codec is correct.
On authorization, retry native creation from that branch, verify the resulting
directory and SHA, and then dispatch the bounded reproduction/correction mission.

Normative adoption of proposal 0011, serialization, exact Identity meaning,
graph behavior, capability comparison, the Macro design-object boundary and
execution as a 0.1.0 acceptance gate remain open. Work on those choices was not
started. The completed preparation does not make 0.1.0 ready for release.

This record follows the content checkpoint. Its own commit and review must be
identified separately in the task history. Production worktrees and branches
are retained for subsequent closure; no cleanup or archival is claimed here.
