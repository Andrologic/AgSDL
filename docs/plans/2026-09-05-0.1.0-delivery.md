# Candidate delivery plan for AgSDL 0.1.0

- Date: 2026-09-05.
- Status: proposed delivery sequence; no release or normative adoption.
- Experimental continuation: 2026-09-06, base `ba13860ae788873ad5db3fa76cc8d752331d101a`.
- Preparation base: `cc0bc6b68d40c0983dbc5ecda0b0d82456eeabc0`.

The maintainer requested continued work until 0.1.0 is ready. The 2026-09-06
dispatch delegates closure of a deterministic D/G/R candidate for experimental
implementations. It does not record ten individual approvals of C1-C10 or adopt
normative syntax. Earlier reviewed work is retained as the basis of this step.
[Proposal 0012](../../proposals/0012-minimal-0.1.0-contract.md) supplies those
choices. [Decision 0004](../decisions/0004-approved-design-directions.md) and the
[repository boundary](../scope.md) remain in force. The maintainer's subsequent
general-purpose direction supersedes the earlier Macro priority and cancels
the pending Macro execution gate. AgSDL has no required product integration,
product-specific mapping or product execution dependency. Selection and adoption
of D/G/R remain open; this scope correction approves no syntax or graph rules.

## Dependencies and observable completion criteria

| Step | Depends on | Work and evidence required to close it |
| --- | --- | --- |
| 1. Close the experimental contract | Candidate 0012 and reviewed conceptual work | Complete candidate-2 field inventories, phase boundaries and rule/location/outcome oracles. Independent review resolves reproducible ambiguities. The candidate marker remains explicit; no C1-C10 approvals are inferred. |
| 2. Prepare experimental fixtures and schemas | Step 1 | Derive schemas from candidate rules and cover each rule with positive, negative and applicable unknown/excluded cases. Record the exact proposal revision as oracle, not a normative specification version. |
| 3. Implement two independent readers | Step 2 | Two separately implemented validation paths with source revisions and dependencies, for example Python and TypeScript, run the candidate fixtures. Shared specification/fixtures are allowed; wrapping one validator or SDK twice is not independence. No described agent or engine is executed. |
| 4. Compare experimental evidence | Step 3 | Compare phase verdicts, rule/location/outcome tuples, deferrals, unknowns, inventory slices and exact exchange. Resolve every mismatch or retain it as a blocker. Record source revisions, coverage and exclusions without claiming official conformance. |
| 5. Review adoption | Steps 1-4 | Present the candidate and experimental evidence for a separate adoption decision. Record accepted/amended/deferred scope and reconcile 0002/0003/0011 with Decisions 0001/0004. No product integration determines the result. Changes after review need updated fixtures and evidence. |
| 6. Apply adopted text and evidence | Step 5 | If adopted, place reviewed semantics in spec, derive official schema/diagnostic identities and rerun both implementations against the adopted edition. Experimental reports cannot be relabelled as normative results. |
| 7. Release review | Steps 1-6 | Scope-labelled evidence, limitations and release notes match the adopted units; repository checks pass and independent review is resolved. Tagging/publication requires its own authorization and is not performed by this plan. |

The delegated experiment prepares D/G/R; final adoption can still select a
smaller release scope. G requires sequence, true/false, input/output, approval/refusal,
timeout, terminal, cycle and missing-dependency fixtures. A negative approval
fixture routes both approved and denied to the protected invocation; G rejects
it because the approved edge must be its only incoming edge. Another negative
fixture routes an invocation's success and failure to a success terminal that
consumes that invocation's output; G rejects the unavailable-output path.
R needs absent selection, custom engines, unverified satisfied/unsatisfied
assertions and unknown evidence hashes without engine fallback or readiness
claims. D/G fixtures distinguish opaque payloads from interpreted fields, null
dependency hashes from missing bytes, and direct resolution from unsupported
transitive references. The [acceptance scenarios](../research/0.1.0-acceptance-scenarios.md)
and [evidence checklist](../research/0.1.0-implementation-checklist.md) are
preparation inputs, not evidence that these tests already ran.

The two-validator result is bounded structural evidence. It proves neither
runtime equivalence nor execution support in any third-party implementation.
Future execution conformance needs a separately reviewed general contract and
observed runs; it is not a product-integration workstream in this delivery plan.
External interface research informs comparisons without prescribing a supplier
or deriving portable requirements from one product.

## Studio item 34 disposition

The [preparation record](2026-09-05-version-0.1.0-preparation.md) describes the
historical startup blocker for Studio item 34. The user subsequently abandoned
that lot because no AgSDL test had been identified. Studio may be a secondary
tool only for an identified AgSDL experiment with its own scope and evidence;
generic codec maintenance is not this contract's deliverable.

The separate Studio candidate `d507d73e400ec77d5b9449646180a2a2ac00aaf4`, based on
`6d8c12de423a1583dbc4841cacf1a3b4d7a78f8d`, is retained on Studio's
`feature/010-codec-corrections` branch. It is left apart, neither integrated nor
reverted by this work, and is not counted as AgSDL evidence. This disposition
supersedes the earlier instruction to retry that lot for this delivery; the
historical preparation record remains unchanged.

## Current step and handoff

The current continuation owns proposal 0012 and this plan only.
It is complete when candidate-2 can be implemented without unspecified covered
field or verdict behavior,
repository verification passes and an independent fresh-context reviewer has
checked the candidate against the stated base and decisions. The task handoff
records the commit SHA, checks and reviewer identity/base/candidate separately.
Those records establish documentation review, not completion of steps 1-7.

No merge, push, release tag, publication, external runtime or other-repository
change is part of this step. The orchestrator controls integration.
