# Candidate delivery plan for AgSDL 0.1.0

- Date: 2026-09-05.
- Status: proposed delivery sequence; no release or normative adoption.
- Experimental continuation: 2026-09-06, base `ba13860ae788873ad5db3fa76cc8d752331d101a`.
- Preparation base: `cc0bc6b68d40c0983dbc5ecda0b0d82456eeabc0`.

The current modular gates are recorded at the end of this plan, with a dated
pre-comparison snapshot. The initial dispatch and candidate-2 sequence below
preserve their historical scope; they do not describe current modular evidence.

The maintainer requested continued work until 0.1.0 is ready. The 2026-09-06
dispatch delegates closure of a deterministic D/G/R candidate for experimental
implementations. It does not record ten individual approvals of C1-C10 or adopt
normative syntax. Earlier reviewed work is retained as the basis of this step.
[Proposal 0012](../../proposals/0012-minimal-0.1.0-contract.md) supplies those
choices. [Decision 0004](../decisions/0004-approved-design-directions.md) and the
[repository boundary](../scope.md) remain in force. The maintainer's subsequent
general-purpose direction supersedes the earlier Macro priority and cancels
the pending Macro execution gate. AgSDL has no required product integration,
product-specific mapping or product execution dependency. Selection and adoption
of D/G/R remain open; this scope correction approves no syntax or graph rules.

## Historical candidate-2 sequence

| Step | Depends on | Work and evidence required to close it |
| --- | --- | --- |
| 1. Close the experimental contract | Candidate 0012 and reviewed conceptual work | Complete candidate-2 field inventories, phase boundaries and rule/location/outcome oracles. Independent review resolves reproducible ambiguities. The candidate marker remains explicit; no C1-C10 approvals are inferred. |
| 2. Prepare experimental fixtures and schemas | Step 1 | Derive schemas from candidate rules and cover each rule with positive, negative and applicable unknown/excluded cases. Record the exact proposal revision as oracle, not a normative specification version. |
| 3. Implement two independent readers | Step 2 | Two separately implemented validation paths with source revisions and dependencies, for example Python and TypeScript, run the candidate fixtures. Shared specification/fixtures are allowed; wrapping one validator or SDK twice is not independence. No described agent or engine is executed. |
| 4. Compare experimental evidence | Step 3 | Compare phase verdicts, rule/location/outcome tuples, deferrals, unknowns, inventory slices and exact exchange. Resolve every mismatch or retain it as a blocker. Record source revisions, coverage and exclusions without claiming official conformance. |
| 5. Review adoption | Steps 1-4 | Present the candidate and experimental evidence for a separate adoption decision. Record accepted/amended/deferred scope and reconcile 0002/0003/0011 with Decisions 0001/0004. No product integration determines the result. Changes after review need updated fixtures and evidence. |
| 6. Apply adopted text and evidence | Step 5 | If adopted, place reviewed semantics in spec, derive official schema/diagnostic identities and rerun both implementations against the adopted edition. Experimental reports cannot be relabelled as normative results. |
| 7. Release review | Steps 1-6 | Scope-labelled evidence, limitations and release notes match the adopted units; repository checks pass and independent review is resolved. Tagging/publication requires its own authorization and is not performed by this plan. |

The delegated experiment prepares D/G/R; final adoption can still select a
smaller release scope. G requires sequence, true/false, input/output, approval/refusal,
timeout, terminal, cycle and missing-dependency fixtures. A negative approval
fixture routes both approved and denied to the protected invocation; G rejects
it because the approved edge must be its only incoming edge. Another negative
fixture routes an invocation's success and failure to a success terminal that
consumes that invocation's output; G rejects the unavailable-output path.
R needs absent selection, custom engines, unverified satisfied/unsatisfied
assertions and unknown evidence hashes without engine fallback or readiness
claims. D/G fixtures distinguish opaque payloads from interpreted fields, null
dependency hashes from missing bytes, and direct resolution from unsupported
transitive references. The [acceptance scenarios](../research/0.1.0-acceptance-scenarios.md)
and [evidence checklist](../research/0.1.0-implementation-checklist.md) are
preparation inputs, not evidence that these tests already ran.

The two-validator result is bounded structural evidence. It proves neither
runtime equivalence nor execution support in any third-party implementation.
Future execution conformance needs a separately reviewed general contract and
observed runs; it is not a product-integration workstream in this delivery plan.
External interface research informs comparisons without prescribing a supplier
or deriving portable requirements from one product.

## Studio item 34 disposition

The [preparation record](2026-09-05-version-0.1.0-preparation.md) describes the
historical startup blocker for Studio item 34. The user subsequently abandoned
that lot because no AgSDL test had been identified. Studio may be a secondary
tool only for an identified AgSDL experiment with its own scope and evidence;
generic codec maintenance is not this contract's deliverable.

The separate Studio candidate `d507d73e400ec77d5b9449646180a2a2ac00aaf4`, based on
`6d8c12de423a1583dbc4841cacf1a3b4d7a78f8d`, is retained on Studio's
`feature/010-codec-corrections` branch. It is left apart, neither integrated nor
reverted by this work, and is not counted as AgSDL evidence. This disposition
supersedes the earlier instruction to retry that lot for this delivery; the
historical preparation record remains unchanged.

## Historical candidate-2 evidence and adoption preparation

Snapshot: 2026-09-06, source revision
`4a43c5ee645b271b5640bd780b109c16ea380b08`.
The experimental candidate-2 contract, its three schemas, the 121-case corpus
and independent Python and JavaScript readers are integrated. Comparison-3 and
a fresh run on this clean revision both report 121 cases, no blocked fixtures
and no failures. The [adoption review dossier](../reviews/0006-candidate-0.1.0-adoption.md)
records exact source revisions and hashes, reproducible commands, retained
reports and limitations. Its comparison-2 snapshot remains explicitly historical.

| Step | Status at the snapshot |
| --- | --- |
| 1. Experimental contract | Candidate-2 integrated, including the final report-boundary clarifications; still experimental. |
| 2. Fixtures and schemas | Three schemas and 121 cases integrated, pinned to the clarified candidate source. |
| 3. Independent readers | Python and JavaScript integrated, with implementation provenance in their READMEs; 54 Python and 37 JavaScript tests pass. |
| 4. Compare evidence | Complete for the pinned experimental corpus. All 121 cases agree on reports and satisfy the targeted assertions, including slices and exact exchange. This is not exhaustive semantic validation or official conformance. |
| 5. Review adoption | Dossier prepared for separate maintainer decisions. The then-open scope and identity questions are recorded historically; the current dossier below supersedes the G/R direction choices. |
| 6. Apply adopted text and evidence | Pending adoption. No normative identity or experimental result is relabelled. |
| 7. Release review | Pending adopted text and renewed evidence. Tagging/publication remains separately authorized. |

Repository checks and reader tests pass at the source revision. Changes to the
contract, fixtures or readers require renewed evidence. This result closes the
candidate-2 comparison step only; it does not adopt D/G/R. The modular
continuation below supersedes its earlier G/R direction questions. Version 0.1.0 remains undelivered.


## Modular MVP continuation, 2026-09-06

The maintainer's subsequent interview sets a general-purpose 0.1.0 POC/MVP
with reusable definitions, separate configurations and concrete executions,
per-Agent engine choices, explicit tool implementations and reusable content
where compatible. Required capabilities cannot be silently removed or replaced;
unknown support is not ready. Configurations are fixed during an execution:
stop, edit and start again. No required product, hot reload, state migration,
universal compatibility verifier or real-engine proof is introduced.

[Proposal 0013](../../proposals/0013-modular-mvp-contract.md) records these
directions and a new experimental edition. Candidate-2 and its 121-case evidence
above remain a completed historical experiment, not evidence for the modular
candidate. No previous comparison is relabelled. Configuration-level Principal rebinding remains
an adoption question; the candidate explicitly retains the existing Principal
definition constraint without claiming that the interview decided it.

| Point | Lot A artifact and criterion | Next evidence |
| --- | --- | --- |
| 36 | 0013 scope distinguishes recorded interview directions from proposed fields and normative adoption. | Maintainer review of the candidate, not a release claim. |
| 37 | Terms and Principal section separates Agent, engine, configuration and execution, with a stated proposed actor boundary. | Decide any broader Principal rebinding before adoption. |
| 38 | Closed configurations, explicit selected id and per-Agent assignments leave graph definitions unchanged. | Lot B selection/coverage oracles. |
| 39 | Tool contracts, implementation choices, claims and declared assessment outcomes expose gaps without readiness. | Lot B incompatible, unknown and not-provided witnesses. |
| 40 | Instructions/Skill payloads and Application ordering declare prerequisites without implicit inheritance or engine priority. | Lot B dependency/order and opaque-parameter cases. |
| 41 | Addressable Interface operations select Action and Ports at invoke. | Lot B operation selection, ambiguity and direction cases. |
| 42 | Explicit call links compose sequential approval gates with expiry and refusal boundaries. | Lot B chain, bypass, data availability and expiry-declaration cases. |
| 43 | Delta grammar, rules, locations, inventory and derived experimental schemas provide an implementation candidate. | Lot B examples/oracles, then independent readers and renewed comparison. |

Lot A changes no reader, comparator or executed corpus. Its independent audit
covers all eight points together. Lot B derives examples/oracles only after
review of the exact candidate revision. Implementation and comparative evidence
follow; adoption and publication remain separate decisions. A structural
candidate does not settle runtime support, authority or timing feasibility.


## Current modular delivery gates, points 47/49

Snapshot: 2026-09-06, preparation base
`b5c97f692d3f9446988f0a79c07412c0b558e585`. This section governs the modular
continuation; the candidate-2 sequence and comparison above are historical.
The [adoption dossier](../reviews/0006-candidate-0.1.0-adoption.md) is the source
for the dated evidence inventory, acquired directions and remaining Principal
choice. Several operations, sequential approval gates and per-Agent configuration
are established needs, not questions to ask again during delivery preparation.

| Gate | Current status | Evidence required to close |
| --- | --- | --- |
| Modular contract and corpus | A `280347e` and B integrated; 26 cases after `1c796de9`, including direct annex resolution. Still experimental. | Preserve the exact proposal, inherited edition, schema and fixture pins for subsequent runs. |
| Independent modular readers | Python `512056a` integrated and validated; JavaScript still active at this snapshot. | Complete the separate JavaScript path and its independent review; retain both source revisions and provenance. |
| Modular comparison | Not yet performed. | Run both paths against the same 26-case revision, preserve raw reports and summary, and resolve every mismatch or record a blocker. A later evidence lot updates this snapshot. |
| 47. Adoption preparation | This documentary lot prepares the questions and recommendation. Adoption remains pending. | Attach comparison evidence, record accept/amend/defer for the exact units and Principal constraint, and account for all established MVP needs. Any semantic amendment requires renewed evidence. |
| Apply adopted contract | Pending adoption; outside this lot. | Reconcile conceptual proposals, place adopted semantics in spec, derive official schemas and identities, update fixtures/readers, and replay against the adopted edition. Keep experimental evidence under its original marker. |
| 49. Delivery review preparation | Review sequence prepared here; final delivery review remains pending. | Verify the adopted scope against the applied text, both reader revisions, fresh reports, coverage limits and release notes. Repository checks and an independent whole-delivery review must pass with all founded defects resolved. |
| Publication | Pending separate authorization. | Obtain explicit authorization for publication actions after delivery review; preparation creates no tag or release. |

Points 47/49 in this lot close only when the two documents agree, identify the
remaining choices without reopening settled needs, pass `./scripts/check.sh`,
and receive a favorable independent review of the complete diff at a stable
SHA. This is preparation evidence, not completion of adoption or final delivery.
No spec, schema, reader or fixture changes belong to this step.

When the later evidence lot updates the dossier, it must preserve the dated
pre-comparison record or label its replacement explicitly, retain prior failed
reports, and state precisely which new revision was tested. A successful
experimental comparison would still establish neither runtime enforcement nor
interoperability. Version 0.1.0 remains undelivered until the later gates close.
