# Local preparation record for AgSDL 0.0.2

- Date: 2026-09-05
- Scope: the maintainer-authorized preparation of a non-normative conceptual
  candidate, as recorded in [Decision 0005](../decisions/0005-version-0.0.2.md).
- Starting `develop`: `b1c22aa4bcd880136dc3bd3c46ef9db19237a716`.
- Previous release: `v0.0.1`, peeled commit
  `53d9880c80c9434d9a287dea677c94880393460e`.
- Integrated content checkpoint:
  `a06115ab5754f84ee3995ffcaa75194645a5bc0c` on local `develop`.

This record preserves the two production lots' review and integration evidence.
The checkpoint precedes this record and its release-note link. It is neither a
release tag nor a claim of publication. The later documentation commit and
merge are identifiable in Git history.

## Execution and ownership

Both tasks ran on host `local` in distinct native Codex worktrees. Their initial
read-only environment checks showed clean detached worktrees at the previous
release. Each then created its feature branch from the verified starting
`develop`. Two production tasks were the maximum for this preparation; neither
started a server or browser. The orchestrator reserved independent review and
serialized merges into the clean main checkout, which remained on `develop`.

| Lot | Native task | Worktree | Branch | Model |
| --- | --- | --- | --- | --- |
| A | `01a07259-3687-7863-8db8-249ff50902a7` | `~/.codex/worktrees/3f9e/AgSDL` | `feature/prepare-0.0.2` | GPT 5.6 Sol, high |
| B | `01a07259-4a68-7c41-9c5c-b609c5b4b5a2` | `~/.codex/worktrees/04f1/AgSDL` | `feature/review-0.0.2` | Astra, medium |

A owned README, ROADMAP, Decision 0005, and the candidate release notes.
B owned the cumulative review report. After the orchestrator checked its two
findings against proposal 0002, B also received the two precise A2UI passages.
Both lots are integrated. Their worktrees were subsequently removed and their
tasks archived during the closure below. Their branches are retained.

## Independent review and integration

Auditor `/root/review_release_002`, Astra with medium reasoning, started with a
fresh context and worked in read-only mode. Its reports are summarized below;
the exact reviewed revisions and boundaries are retained.

| Lot | Reviewed candidate | Local merge |
| --- | --- | --- |
| A | `47ff6e9ee15cf74974647272d26a0e5ee244d3dc` | `c4278d8c9a3890261e0851e3d770926762797a8a` |
| B | `e11fec8d5bf0fa2151a1c331de949206c578ae33` | `a06115ab5754f84ee3995ffcaa75194645a5bc0c` |

Both candidates use the starting `develop` above as their change base.

- A received a favorable review with no reproducible defect. The auditor
  checked the four-file boundary, cumulative summary, eleven proposed
  proposals, six bindings without implementation claims, and the distinction
  between a local candidate and a published version. It found no missing local
  validation for this documentation step.
- B received a favorable review with no reproducible defect. The auditor
  checked both corrections against proposal 0002, important report assertions,
  and the exact 44-path coverage inventory. It also assessed interaction with
  the integration base `c4278d8c9a3890261e0851e3d770926762797a8a` and found no
  documentary conflict. This independent review did not repeat the entire
  cumulative investigation.
- The orchestrator read both diffs, checked the corrected source distinctions,
  independently compared the 44-path inventory with Git, and approved each
  candidate before its merge. A and B modified distinct files. Neither merge
  required conflict resolution.

The [cumulative qualification report](../reviews/0005-release-0.0.2.md)
preserves B's original review boundary and its two findings. Correction commit
`356decb96e50190e258a2f8c618f50548539255d` is included in B's reviewed candidate.

## Validation and remaining boundary

`./scripts/check.sh` passed on the starting tree, on the task candidates, during
independent review, and after each integration. It exercised the local A2A
verifier regression cases and seven Markdown checker tests, along with the
repository checks documented in CONTRIBUTING. These are maintenance checks,
not an AgSDL conformance suite.

At the integrated checkpoint, `develop` was clean, `main` remained at the
previous release commit, and the only local tag was `v0.0.1`. No task or
orchestrator action in this preparation pushed, tagged, published, deployed,
or changed another repository. External publication state and current upstream
sources were not independently queried.

Local preparation is complete for this conceptual scope. Publication remains
a separate maintainer action. The first implementable contract, graph behavior,
serialization, and Macro or Agent Graph Studio integration remain outside this
work; no pending choice for `0.1.0` was settled here.

## Closure on 2026-09-05

The maintainer requested closure of the `0.0.2` chantier. The two native tasks
above are its complete task inventory. The principal orchestrator remains open;
the audit sub-agent is not a separate production task to archive. Earlier
worktree registrations from other AgSDL work are outside this cleanup.

Closure started from clean `develop` at
`1ad82ed25bc66fb340dc638a21d23aa59d1d5601`. The actual independent A/B reports
were reused: their candidate SHAs were unchanged, all their commits were
ancestors of this destination, and no blocking finding remained. The
orchestrator verified the native task states, exact worktrees, Git common
directory, branches, HEADs, and inclusion in `develop`, and approved closure
against the original missions. No duplicate merge was performed.

| Lot | Completed native turn | Worktree removal | Native archival |
| --- | --- | --- | --- |
| A | `01a07260-6943-7a22-ae63-ca7591dd148f` | Succeeded; directory and Git registration absent | Tool confirmed `archived: true` for the exact task ID above |
| B | `01a0725b-6d62-7641-b1b3-af9c7f8b6971` | Succeeded; directory and Git registration absent | Tool confirmed `archived: true` for the exact task ID above |

Both tasks were idle with completed turns before cleanup. Tracked and untracked
status, plus the ignored-file inventory, were empty. No local artifact needed
preservation beyond the committed reports and this orchestrator record.

`lsof` identified task tool processes retaining each worktree as their working
directory. After checking each PID's exact directory again, the orchestrator
sent SIGTERM only to those processes. A's PIDs were 15643, 15644, 15645, 15646,
15693, 15798, and 15799; B's were 15995, 15996, 15997, 16003, 16029, 16152,
and 16153. Child PIDs 15693 and 16029 had already exited when reached. The shared
application parent was preserved. A subsequent `lsof` scan found no remaining
worktree users.

The orchestrator then ran `git worktree remove` on each exact path, sequentially
and without force, verified both filesystem and Git-registration absence, and
only then archived the corresponding explicit native task ID on host `local`.
The native tools confirmed both archival operations. Branches were not deleted.

The earlier coordination commit
`dc0dee7bdc32058f44ff4aa6a6c3c17052a40e16`, based on
`a06115ab5754f84ee3995ffcaa75194645a5bc0c`, also received a favorable independent
review from `/root/review_release_002` before merge
`1ad82ed25bc66fb340dc638a21d23aa59d1d5601`. That review covered the preparation
record and release-note links. This closure update is a later documentation
step, not implicitly covered by those earlier reviews.

The orchestrator's temporary coordination worktree is used to commit this
update. It has no separate native task to archive and is eligible for removal
after this update's own review, integration, and validation. The principal
checkout and task remain available. Closure creates no release tag, publication,
push, deployment, or normative adoption.
