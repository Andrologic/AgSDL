# AgSDL 0.1.0 delivery-readiness review

- Date: 2026-09-07.
- Status: E49 closed for the bounded local first draft; publication pending.
- Delivery integration base: `6bc8ebb070e9542aa32fb53f01fc4c91349a737e`.
- Python reader lot: `9293a8c`.
- Official corpus lot: `b8a4d1b`.

This review closes local delivery gate E49 after the E47 adoption recorded by
Decision 0007. It does not authorize publication or claim runtime conformance.

## Delivered scope

- `spec/` contains the adopted D/G/R contract and official identities.
- `schemas/` contains the derived closed shapes.
- independent Python and JavaScript readers implement the seven operations;
- `conformance/` contains 137 pinned official cases and a neutral comparator;
- `examples/0.1.0/` contains one official, non-normative D/G/R example; and
- public and contributor documentation states the first draft's limits.

The final JavaScript correction preserves a definitive D-AGENT cardinality
finding beside an unreadable relation and counts only correctly typed `actsAs`,
`exposes` and `directedBy` relations toward official Agent minima. The latter
fix resolves `candidate2-relation-kind-contract`. Historical candidate markers
and corpora remain unchanged, and both historical comparisons still pass. The
shared partial-diagnostic correction may also affect combinations outside those
historical corpora.

## Evidence

| Evidence | Result |
| --- | --- |
| Official manifest | 137 cases; SHA-256 `be6987fe7eee3ee8c990aa5ee57dab59c9ce5b6bcc5d45c5334b16ad9c8bc52e`. |
| Official comparison | Python and JavaScript; 137 cases; no blocked case, failure or mismatch. |
| Emitted report shapes | All 274 responses validate against `schemas/report.schema.json`. |
| Retained summary | SHA-256 `6720f8caaf00ceb9a6cb1485dd78060ad2fe7fa00fd8421d66ebc79f08208873`. |
| Historical comparisons | Candidate-2 121/121 and modular 26/26 pass under their own markers. |
| Official example | Both readers agree on all seven operation outcomes; D/G/R schemas pass. |
| Repository and reader checks | `./scripts/check.sh`, six reader suites and `git diff --check` pass. |
| Independent delivery audit | Favorable review required against the committed N5 diff; its external record identifies the reviewed SHA. |

The retained comparison stores stdout and stderr for every reader and case. Its
summary covers report oracles, exact locations and slices, inventory states and
exchange bytes. The comparator supplies no semantic oracle of its own.

## Gate disposition

| Gate | Disposition |
| --- | --- |
| E47 adoption and normative application | Closed locally by Decision 0007 and the integrated specification. |
| Independent reader implementations | Closed for the bounded seven-operation scope. |
| Official corpus and cross-reader comparison | Closed with the evidence above. |
| N5 documentation, example and JavaScript fixes | Closed after repository checks and independent review. |
| E49 local delivery readiness | Closed for 0.1.0's acquired MVP scope. |
| Publication | Open; explicit maintainer authorization required. |

## Evidence limits

The corpus is finite. Agreement may preserve a shared mistake outside covered
combinations. JSON Schema passes check shape, not semantic validity. Placeholder
evidence hashes in the example are declarations, not verified evidence.

This review does not claim exhaustive correctness, whole-model conformance,
execution support, engine compatibility, evidence authenticity or
interoperability. Any broader language or runtime work requires a later proposal
and evidence appropriate to that scope.
