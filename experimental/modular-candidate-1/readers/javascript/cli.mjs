#!/usr/bin/env node
import {run,stringify} from './reader.mjs';
function transport(x){if(!x||typeof x!=='object'||Array.isArray(x))throw new TypeError('request must be an object');const decode=(v,name)=>{if(typeof v!=='string'||!/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/.test(v))throw new TypeError(`${name} must be canonical base64`);const b=Buffer.from(v,'base64');if(b.toString('base64')!==v)throw new TypeError(`${name} must be canonical base64`);return b;};const annexes=Object.create(null);if(!x.annexes||typeof x.annexes!=='object'||Array.isArray(x.annexes))throw new TypeError('annexes must be an object');for(const[k,v]of Object.entries(x.annexes))annexes[k]=decode(v,`annex ${k}`);const y=run({...x,primary:decode(x.primary,'primary'),annexes}),artifacts=Object.fromEntries(Object.entries(y.artifacts).map(([k,v])=>[k,Buffer.from(v).toString('base64')]));return{report:y.report,artifacts};}
let input='';for await(const chunk of process.stdin)input+=chunk;try{const request=JSON.parse(input);process.stdout.write(stringify(transport(request))+'\n');}catch(error){process.stderr.write(`host request error: ${error.message}\n`);process.exitCode=2;}
